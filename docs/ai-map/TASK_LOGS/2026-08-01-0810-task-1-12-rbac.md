# Task: Task 1.12 PIN authentication and RBAC

Date: 2026-08-01 08:10 UTC
Repo: `/home/longnick/projects/small-pos`
Branch: `main`
AI/Agent: Hermes Agent
User request: Implement Phase 1 Task 1.12 with strict TDD and one bounded commit.

## Before state

- Existing branch: `main` at `db5e6fe feat(core): add safe shift report computation`.
- Existing uncommitted files: none.
- Relevant AI map files read: `docs/ai-map/LOVABLE_COMPONENT_MAP.md`, `docs/ai-map/DEMO_SEED_CONTRACT.md`.
- Relevant source files read: `packages/pos-core/src/types.ts`; existing core test style.
- Backup dir: none; target files did not exist before task.

## Goal

Thêm xác thực PIN 4 chữ số qua verifier được inject và guard quyền theo role, fail closed.

## Files changed

- `packages/pos-core/src/auth.ts` — exports `verifyPin` and `canAccess`.
- `packages/pos-core/test/auth.test.ts` — 17 focused behavior cases.
- `docs/ai-map/CODE_MAP.md` — records core auth module.
- `docs/ai-map/FILE_RELATIONS.md` — records auth dependencies.
- `docs/ai-map/CHANGELOG_AI.md` — records completion.
- `docs/ai-map/TODO_AI.md` — records next scope and Notion blocker.
- This task log — handoff evidence.

## Code relations

- `packages/pos-core/src/auth.ts` imports `Role` from `packages/pos-core/src/types.ts`.
- `packages/pos-core/test/auth.test.ts` tests `verifyPin` and `canAccess` directly.
- Future login/action adapters may call this module; no UI, DB, store, bcrypt, or cloud wiring added.

## Code Anchors

- `packages/pos-core/src/auth.ts::verifyPin()` — validates four ASCII digits, requires hash/verifier, contains sync/async verifier errors. Marker: `if (!/^\d{4}$/.test(pin) || !pinHash || !verifier) return false;`.
- `packages/pos-core/src/auth.ts::canAccess()` — validates dotted action and applies exact/family rules with boundary-safe prefix. Marker: `permission.endsWith(".*")`.
- `packages/pos-core/test/auth.test.ts` — authorization and verifier regression suite. Marker: `wildcards match family boundary only`.

## Decisions made

- Used injected `boolean | Promise<boolean>` verifier. No crypto dependency per scope.
- Accepted actions only as two lowercase/underscore segments separated by one dot.
- Unknown roles, malformed actions, missing hashes/verifiers, throws, and rejections return false.

## Verification

- `npx vitest run packages/pos-core/test/auth.test.ts` before source module → exit 1; expected RED: `Failed to resolve import "../src/auth"`.
- `npx vitest run packages/pos-core/test/auth.test.ts` after source module → PASS (17), FAIL (0).
- `npm run ci:e2e` → PASS: `tsc --noEmit`, 14 unit files / 55 tests, Vite build, leakage scan, Playwright 3 tests.
- `git diff --check` → exit 0.

## Remaining issues

- No bcrypt adapter, login flow, UI/store/DB integration by explicit scope.
- Notion update not attempted; credentials/access unavailable and prohibited by task.

## Next step

Continue next approved Phase 1 task. Wire this core API only under later integration task.

## Safety notes

No secrets, database, migration, POS data, or raw media were touched.

## Rollback

- Revert bounded commit `1410f209e5e5055970e83db8fc29570d3c2fe8df`.
