# Task: Dexie read-only hydrate

Date: 2026-08-14 07:27 UTC
Repo: `/home/longnick/projects/small-pos-dexie-hydrate`
Branch: `feat/dexie-hydrate`
AI/Agent: Hermes / gcli/grok-4.6
User request: `Duyệt làm tiếp` → owner named hydrate impl (Later #1, read-only)

## Before state

- `origin/main` `cf51044` — design #27 merged
- Later #1 still `[ ]`
- No `src/pos/dexie-hydrate.ts`

## Goal

Hydrate catalog/tables từ Dexie v1 sau auth. Không persist. Không restore order. Không đổi `bootstrapDemoPos()`.

## Files changed

- `src/pos/dexie-hydrate.ts` — read-only hydrate
- `src/pos/dexie-hydrate.test.ts` — TDD
- `src/App.tsx` — else-branch khi `pos` null
- `src/App.test.tsx` — mock + boot test
- `src/order-entry-app.test.tsx` — mock hydrate null
- docs/ai-map

## Code relations

- `App` gọi `hydrateFromDexie({ authenticatedTenantId: t.id })` sau `bootstrapDemoAuth()`
- Hydrate đọc `PosDatabase` 4 store / 1 transaction
- Commit store chỉ qua `replaceTenantData`

## Decisions made

- `databaseName` optional cho test isolation. Production mặc định `small-pos`.
- Hydrate không ghi Zustand. App mới `replaceTenantData`.
- Later #1 vẫn `[ ]` đến khi PR merge + review.

## Verification

- RED: missing module, rồi App chưa gọi hydrate
- GREEN: hydrate + App + order-entry focused tests
- `npm run ci` pending

## Remaining issues

- Persist / currentOrder / multi-tab / migration: Later #2–#4
- ROADMAP Later #1 unchecked until this PR merges

## Next step

Sol `cx/gpt-5.6-sol`. PR. Không merge.

## Safety notes

No secrets, database, migration, POS data, or raw media were touched.
