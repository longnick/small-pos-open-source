# Task: Dexie Later #3 multi-tab CAS + BroadcastChannel

Date: 2026-08-15
Repo: `/home/longnick/projects/small-pos-dexie-tabs`
Branch: `feat/dexie-tabs`
AI/Agent: Hermes / gcli/grok-4.6
User request: Làm cho xong 100% (close Later #3 after #34 merge)

## What shipped

- CAS inside persist transactions: occupy refuses a table already bound to another order; edit refuses a paid/non-open Dexie order; pay refuses a table occupied by another order.
- Same-id pay replay stays idempotent.
- After a successful write, `notifyDexieTabWrite({ tenantId, kind })` on `BroadcastChannel("small-pos-dexie")`.
- `PosShell` listens and rehydrates catalog/tables for the same tenant. Does not swap `currentOrder`.
- Design: `docs/dexie-tabs.md`. ROADMAP Later #3 `[x]`.

## Verification

- RED then GREEN: occupy-other-order, edit-paid, pay-idempotent, tab notify/listen.
- App: other-tab persist event rehydrates tables.
- Issue #1 / PR #31 untouched.

## Next

Sol `cx/gpt-5.6-sol` then PR. Do not merge here.
