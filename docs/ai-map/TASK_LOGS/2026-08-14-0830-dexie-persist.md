# Task: Dexie persist write-back impl

Date: 2026-08-14 08:30 UTC
Repo: `/home/longnick/projects/small-pos-dexie-persist-impl`
Branch: `feat/dexie-persist`
AI/Agent: Hermes / gcli/grok-4.6
User request: Làm tiếp phần code đi, tự đặt tên nhánh

## Before state

- Existing branch: `feat/dexie-persist` from `origin/main` `787e940`
- Existing uncommitted files: none at start
- Relevant AI map files read: TODO_AI, CHANGELOG_AI, CODE_MAP, FILE_RELATIONS, DECISIONS
- Relevant source files read: `docs/dexie-persist.md`, `src/pos/dexie-hydrate.ts`, `src/App.tsx`, MenuGrid, OrderPanel, PaymentModal, stores

## Goal

Ghi Dexie sau occupy / sửa đơn / thanh toán. Chỉ khi session hydrate thành công. Không restore currentOrder. Later #1 vẫn `[ ]`.

## Files changed

- `src/pos/dexie-persist.ts` — occupy/order/pay writers
- `src/pos/dexie-persist.test.ts` — fake-indexeddb accept/abort
- `src/App.tsx` — session flag + occupy/pay hooks
- `src/App.test.tsx` — hydrate/empty/E2E persist gates
- `src/components/pos/MenuGrid.tsx` — persistAfterOrderEdit
- `src/components/pos/OrderPanel.tsx` — persistAfterOrderEdit
- docs/ai-map

## Code relations

- App else-branch hydrate sets `setDexiePersistSession(true)`
- Occupy/pay call persist from App after Zustand success
- MenuGrid/OrderPanel call persistAfterOrderEdit after mutator true

## Decisions made

- Session flag, not “IDB exists”
- Pay: release then snapshot then persist as-is
- Dexie fail does not roll back Zustand
- No ROADMAP checkbox

## Verification

Focused persist + App + hydrate + UI tests. Full `npm run ci` pending.

## Remaining issues

Sol review. Owner merges. Later #1 still `[ ]`.

## Next step

Sol review, open PR, stop. Do not merge.

## Safety notes

No secrets, database, migration, POS data, or raw media were touched.
