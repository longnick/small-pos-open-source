# Task: Dexie restore occupied-table impl

Date: 2026-08-14 15:10 UTC
Repo: `/home/longnick/projects/small-pos-dexie-restore-impl`
Branch: `feat/dexie-restore`
AI/Agent: Hermes / gcli/grok-4.6
User request: `Làm tiếp đi` after #32 restore design merged.

## Before state

- Existing branch: new worktree from `origin/main` `08d0a8f`
- Existing uncommitted files: none
- Relevant AI map files read: TODO, CODE_MAP, FILE_RELATIONS, DECISIONS
- Relevant source files read: `docs/dexie-restore.md`, `src/App.tsx`, `src/pos/dexie-persist.ts`, `src/stores/order-payment-store.ts`

## Goal

Khôi phục 1 đơn `open` khi bấm bàn occupied / waiting_payment sau hydrate thành công.

## Files changed

- `src/pos/dexie-restore.ts` — read-only loader + classify
- `src/pos/dexie-restore.test.ts` — fake-indexeddb accept/reject
- `src/App.tsx` — occupied branch of `handleTableSelect`
- `src/App.test.tsx` — wiring: restore, missing id, empty IDB, existing currentOrder
- docs/ai-map

## Code relations

- `App.handleTableSelect` calls `loadOpenOrderForTable` then `selectOpenOrder`
- Restore reads Dexie `orders` only. Persist still writes.
- `setSelectedTableId` is React state after successful select

## Decisions made

- No boot restore. No Dexie write. No ROADMAP tick.
- Missing `currentOrderId` = no Dexie read, no highlight.
- Proxy trap: classify via materialize catch; test stubs Dexie transaction.

## Verification

- RED: missing `./dexie-restore`
- GREEN: restore + App tests pass
- Full `npm run ci` pending

## Remaining issues

- Sol review + PR. Do not merge here.
- Later #1/#2 stay `[ ]`.
- Backup UI, paid-occupied repair, multi-tab out of slice.

## Next step

Sol `cx/gpt-5.6-sol`. Open PR. Stop.

## Safety notes

No secrets, database, migration, POS data, or raw media were touched.
Issue #1 / PR #31 untouched.
