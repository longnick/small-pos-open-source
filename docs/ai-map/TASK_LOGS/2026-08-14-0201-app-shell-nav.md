# Task: App shell nav aria-current

Date: 2026-08-14 02:01 UTC
Repo: `/home/longnick/projects/small-pos-app-nav`
Branch: `fix/app-shell-nav`
AI/Agent: Hermes / gcli/grok-4.6
User request: Duyệt làm tiếp App shell nav

## Before state

- Existing branch: new from `origin/main` `885542b` (merged #23)
- App view nav buttons had visible names but no `aria-current` and no `type`
- File comment still says "do not modify"; owner explicitly asked this slice

## Goal

Đánh dấu view hiện tại cho screen reader (`aria-current="page"`) và khóa `type="button"`. Không đổi layout, copy, store, Dexie.

## Files changed

- `src/App.test.tsx` — lock default `Bán hàng` current; click `Bếp` exclusivity
- `src/App.tsx` — `type="button"` + `aria-current` + `aria-hidden` on icons
- `docs/ai-map/*`

## Decisions made

- Used `aria-current="page"` not `aria-pressed` because these switch whole views, not filter chips.
- Owner override of "do not modify" comment. Comment left in place; only nav attrs changed.

## Verification

- RED: `aria-current` null on `Bán hàng` / after click `Bếp`.
- GREEN: App nav tests 2/2 after attrs.
- `npm run ci` — typecheck + 423/423 + build + leakage PASS.
- Sol review: `cx/gpt-5.6-sol` → `gpt-5.6-sol`, verdict APPROVE. No blocking. Non-blocking: icon `aria-hidden` not locked.

## Remaining issues

- Sol review, PR, owner merge. No deploy of this PR until asked.
- MenuManagement search Input still unnamed (`placeholder` is not a name).
- IndexedDB/Dexie later. Issue #1 docs feedback still open.

## Safety notes

No secrets, database, migration, POS data, or raw media were touched.
