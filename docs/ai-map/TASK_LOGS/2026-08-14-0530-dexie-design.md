# Task: Dexie design (Later #1)

Date: 2026-08-14 05:30 UTC
Repo: `/home/longnick/projects/small-pos-dexie-design`
Branch: `docs/dexie-design`
AI/Agent: Hermes / gcli/grok-4.6
User request: next increment = Dexie design PR (schema + hydrate, no adapter)

## Before state

- Existing branch: new from `origin/main` `94bb10f` (merged #26)
- `packages/pos-storage` Dexie v1 + backup tests exist, unused by app
- Production `bootstrapDemoPos()` returns `null`
- Four Zustand stores in-memory only

## Goal

Ghi contract hydrate Dexie v1 → store. Không viết adapter. Không persist. Không schema bump.

## Files changed

- `docs/dexie-design.md` — design
- `ROADMAP.md` — link design, Later #1 still `[ ]`
- `README.md` — pointer
- `docs/ai-map/*`

## Decisions made

- Reuse v1. Read-only first adapter. Empty IDB valid. No auto-seed. Auth stays in-memory.

## Verification

- Docs only. `npm run ci` not required for markdown; run if cheap to prove no src change.
- Sol review after commit.

## Remaining issues

- Owner merge. Then a separate PR may implement read-only hydrate.
- Later #2–#4 untouched.

## Safety notes

No secrets, database, migration, POS data, or raw media were touched. No adapter code.
